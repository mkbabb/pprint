[package]
name = "pretty"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "A small pretty printing library for Rust"

[lib]
name = "pretty"
path = "src/lib.rs"

[dependencies]
pretty_derive = { path = "./derive", version = "0.1.0" }
regex = "1.7.3"
